<!DOCTYPE html>
<html>
  <head>
    <title>User List</title>
  </head>
  <body>
    <h1>User List (For Development)</h1>
    <ul>
      <%
        var list = '';
        for (i = 0; i < userlist.length; i++) {
          list += '<li>' + JSON.stringify(userlist[i])+'</li>';
        }
      %>
      <%- list %>
    </ul>
  </body>
</html>
